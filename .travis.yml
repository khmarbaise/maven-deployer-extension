language: java
sudo: false
jdk:
  - openjdk7
  - oraclejdk8
  - openjdk9
  - openjdk10
cache:
  directories:
  - $HOME/.m2/repository
# Just skip installation step
# cause everything is done
# in script step.
install: yes
script:
  - mvn -B -Prun-its clean verify
